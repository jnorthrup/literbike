[package]
name = "litebike-fuzzer"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
rand = "0.8"
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }

[profile.dev]
opt-level = 0

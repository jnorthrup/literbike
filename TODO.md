# Litebike TODO

## URGENT: Carrier Tethering Bypass
- [ ] Merge FFI Knox bypass branch - BLOCKED: Need to handle uncommitted changes
- [ ] Enable tethering bypass immediately  
- [ ] Configure radio interface detection
- [ ] Deploy universal listener with POSIX peek
- [ ] Add packet fragmentation for DPI evasion
- [ ] Implement protocol obfuscation
- [ ] Test carrier bypass effectiveness

## Proxy Icon Live DSEL Menu
- [x] Turn the menu icon into a DSEL launcher surface instead of a static review stub
- [x] Check in a curated free-lane DSEL pack for GLM5, Kimi K2.5, and NVIDIA
- [x] Add copy-ready lifecycle commands alongside the pragmatic host-block refs
- [x] Cover the checked-in host-block refs with focused pragmatic-route tests
- [x] Load the menu from the DSEL pack instead of hardcoding the quick picks in `index.html`
- [ ] Surface live gateway inventory in the menu, not just a copied `curl` command
- [ ] Show env-key presence and selected binding per lane without exposing secret values
- [ ] Add a real launch/probe action that hits the unified-port lifecycle path from the menu
- [ ] Reflect live readiness, quota, and fallback state in the icon/menu instead of static labels
- [ ] Add a browser-visible smoke path for the menu so UI behavior is validated, not just Rust parsing
- [ ] Tie menu actions to the proxy DSEL/runtime layer instead of leaving them as clipboard helpers
- [ ] Add editing/reload support for operator-maintained DSEL packs under `configs/`

## Notes
- Carrier is clipping tethering - need immediate fix
- FFI branch has Knox bypass techniques ready
- Focus on working solution over perfect code
